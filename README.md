# Prompt Stack Runtimes

Runtime binaries and registry for Prompt Stack CLI.

## Usage

The CLI fetches `registry.json` from releases to discover available runtimes.

```bash
pstack runtime list
pstack runtime ensure python
pstack registry update
```
